# M1 spike frontend PoC: 環境変数サンプル
# 実値は .env.local（git ignore 対象）で管理。

# Backend API base URL (Frontend が fetch する先)
# 結合検証では Backend の ALLOWED_ORIGINS と整合させること
NEXT_PUBLIC_API_BASE_URL=http://localhost:8090
